---
import BaseLayout from "../../layouts/BaseLayout.astro";
import MainNavigation from "../../components/MainNavigation.astro";
import { RecipeForm } from "../../components/RecipeForm";

const title = "New Recipe";
---

<BaseLayout title={title}>
  <div class="bg-bg flex min-h-screen flex-col">
    <main class="container mx-auto max-w-2xl flex-1 px-4 py-12">
      <div class="mb-10 text-center">
        <h1 class="text-text mb-2 text-3xl font-bold">Ny oppskrift</h1>
        <p class="text-text-muted">Legg til en ny favoritt i samlingen din</p>
      </div>

      <RecipeForm
        client:load
        userId={Astro.locals.user.id}
        householdId={Astro.locals.householdId}
      />
    </main>

    <MainNavigation />
  </div>
</BaseLayout>
